apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maas-engine.fullname" . }}-test-connection"
  labels:
    {{- include "maas-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maas-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
