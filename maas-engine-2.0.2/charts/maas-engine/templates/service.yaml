---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "maas-engine.fullname" . }}
  labels:
    {{- include "maas-engine.labels" . | nindent 4 }}
    role: compute
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maas-engine.selectorLabels" . | nindent 4 }}
    role: compute
